{
  "scripts": {
    "setup:cypress": "npm install && npx cypress install && npx cypress verify && npx cypress install-browsers"
  },
  "devDependencies": {
    "cypress": "^15.0.0"
  },
  "dependencies": {
    "cypress-mochawesome-reporter": "^4.0.2"
  }
}