{
  "scripts": {
    "setup:cypress": "npm install && npx cypress install"
  },
  "devDependencies": {
    "cypress": "^15.0.0"
  },
  "dependencies": {
    "cypress-mochawesome-reporter": "^4.0.2"
  }
}